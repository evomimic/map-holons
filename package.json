{
  "name": "map-project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "happ",
    "host"
  ],
  "scripts": {
    "start": "npm run build:happ && npm run start:host",
    "build": "npm run build:happ && npm run build:host",
    "start:host": "npm run start -w host",
    "build:host": "npm run build -w host",
    "build:happ": "npm run build:happ -w happ",
    "clean:ui": "npx rimraf package-lock.json node_modules && npm run clean:ui -w host && npm run clean:ui -w happ",
    "clean:rust": "npx rimraf target .cargo Cargo.lock && npm run clean:rust -w happ && npm run clean:rust -w host"
  },
  "devDependencies": {
    "autoprefixer": "^10.4",
    "concurrently": "^9",
    "rimraf": "^6"
  }
}
